<!DOCTYPE html>
<html lang="en" data-theme="<%= cookies[:magazine_theme] == 'dark' ? 'dark' : 'light' %>">
  <head>
    <title>The Magazine</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>

  <body class="bg-white text-gray-900 font-sans antialiased"
        data-controller="scroll-progress theme mobile-nav">

    <%# ── Top bar: date | social + toggle + subscribe ─────── %>
    <div class="site-banner border-b border-gray-200 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 flex items-center justify-between h-9">
        <%# Date — left %>
        <span class="site-banner-date text-xs tracking-widest text-gray-500 uppercase hidden sm:block">
          <%= Date.today.strftime("%A, %B %-d, %Y") %>
        </span>
        <%# Social icons + subscribe — right %>
        <div class="flex items-center gap-3 ml-auto">
          <%# Social icons %>
          <div class="hidden sm:flex items-center">
            <%# Facebook %>
            <a href="#" aria-label="Facebook" class="social-icon social-facebook">
              <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 24 24">
                <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>
              </svg>
            </a>
            <%# Twitter/X %>
            <a href="#" aria-label="Twitter" class="social-icon social-twitter">
              <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 24 24">
                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
              </svg>
            </a>
            <%# Instagram %>
            <a href="#" aria-label="Instagram" class="social-icon social-instagram">
              <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
                <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
                <circle cx="12" cy="12" r="4"/>
                <circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/>
              </svg>
            </a>
            <%# YouTube %>
            <a href="#" aria-label="YouTube" class="social-icon social-youtube">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
              </svg>
            </a>
          </div>
          <%# Thin divider %>
          <span class="hidden sm:block w-px h-4 bg-gray-200"></span>
          <%# Subscribe %>
          <%= link_to "Subscribe", "#",
            class: "subscribe-btn bg-gray-900 text-white text-xs font-semibold tracking-widest uppercase px-4 py-1.5 hover:bg-gray-700 transition-colors",
            data: { action: "click->scroll-progress#scrollToSubscribe" } %>
        </div>
      </div>
    </div>

    <%# ── Masthead: large centered title ─────────────────── %>
    <header class="site-header border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 py-8 sm:py-10 text-center">
        <div class="flex items-center gap-4 mb-5">
          <span class="flex-1 h-px bg-gray-200 masthead-rule"></span>
          <span class="site-est text-[10px] tracking-[0.3em] text-gray-400 uppercase">Est. 2024</span>
          <span class="flex-1 h-px bg-gray-200 masthead-rule"></span>
        </div>
        <%= link_to root_path do %>
          <h1 class="site-title font-serif text-5xl sm:text-7xl font-bold tracking-tight text-gray-900 leading-none">
            The Magazine
          </h1>
          <p class="site-tagline text-xs tracking-[0.35em] text-gray-400 uppercase mt-3">Stories that matter</p>
        <% end %>
        <span class="block h-px bg-gray-200 masthead-rule mt-5"></span>
      </div>
    </header>

    <%# Sentinel — sits just above the nav; when it leaves the viewport the nav turns dark %>
    <div data-scroll-progress-target="sentinel" aria-hidden="true"></div>

    <%# Nav — dark background, becomes fixed once sentinel leaves viewport %>
    <nav class="site-nav bg-gray-900" data-scroll-progress-target="nav">
      <div class="max-w-7xl mx-auto px-4 sm:px-6">

        <%# ── Desktop nav (md and above) ──────────────────────── %>
        <ul class="hidden md:flex items-center gap-0 text-sm font-medium w-full overflow-x-auto whitespace-nowrap">
          <li>
            <%= link_to root_path,
              class: "site-nav-home flex items-center justify-center px-4 py-3 bg-red-500 text-white hover:bg-red-600 transition-colors border-b-2 border-red-500",
              aria: { label: "Home" },
              title: "Home" do %>
              <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                <polyline points="9 22 9 12 15 12 15 22"/>
              </svg>
            <% end %>
          </li>
          <% Category.joins(:articles).merge(Article.published).distinct.limit(5).each do |cat| %>
            <li>
              <%= link_to cat.name, category_path(cat),
                class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(category_path(cat)) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
            </li>
          <% end %>
          <li>
            <%= link_to "All Articles", articles_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(articles_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <li>
            <%= link_to "Tags", tags_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(tags_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <li>
            <%= link_to "About", about_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(about_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <li>
            <%= link_to "Portfolio", portfolio_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(portfolio_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <li>
            <%= link_to "Contact", contact_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(contact_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <li>
            <%= link_to "Collections", collections_path,
              class: "site-nav-link block px-4 py-3 transition-colors #{current_page?(collections_path) ? 'text-white border-b-2 border-red-500' : 'text-gray-300 hover:text-white'}" %>
          </li>
          <%# Theme toggle — end of desktop nav %>
          <li class="ml-auto flex items-center px-4">
            <button class="theme-float"
                    data-action="click->theme#toggle"
                    aria-label="Toggle theme"
                    title="Toggle dark/light mode">
              <span class="theme-float-track">
                <svg class="theme-float-sun-icon" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="5"/>
                  <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
                </svg>
                <span class="theme-float-knob"></span>
                <svg class="theme-float-moon-icon" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
                </svg>
              </span>
            </button>
          </li>
        </ul>

        <%# ── Mobile top bar (below md) ────────────────────────── %>
        <div class="flex md:hidden items-center justify-between h-12">
          <%# Home icon — left %>
          <%= link_to root_path,
            class: "flex items-center justify-center w-10 h-10 bg-red-500 text-white hover:bg-red-600 transition-colors shrink-0",
            aria: { label: "Home" } do %>
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
          <% end %>

          <%# Site name — center %>
          <%= link_to root_path, class: "font-serif text-white font-bold text-lg tracking-tight" do %>
            The Magazine
          <% end %>

          <%# Theme toggle + hamburger — right %>
          <div class="flex items-center gap-1">
            <button class="theme-float"
                    data-action="click->theme#toggle"
                    aria-label="Toggle theme">
              <span class="theme-float-track">
                <svg class="theme-float-sun-icon" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="5"/>
                  <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/>
                </svg>
                <span class="theme-float-knob"></span>
                <svg class="theme-float-moon-icon" width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
                </svg>
              </span>
            </button>

            <button class="flex items-center justify-center w-10 h-10 text-gray-300 hover:text-white transition-colors"
                    data-action="click->mobile-nav#toggle"
                    aria-label="Toggle menu">
              <%# Hamburger icon %>
              <svg data-mobile-nav-target="hamburger" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="3" y1="6"  x2="21" y2="6"/>
                <line x1="3" y1="12" x2="21" y2="12"/>
                <line x1="3" y1="18" x2="21" y2="18"/>
              </svg>
              <%# Close (X) icon %>
              <svg data-mobile-nav-target="closeIcon" class="w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="18" y1="6"  x2="6"  y2="18"/>
                <line x1="6"  y1="6"  x2="18" y2="18"/>
              </svg>
            </button>
          </div>
        </div>

      </div>
    </nav>

    <%# ── Mobile full-screen overlay — separate component ────── %>
    <div data-mobile-nav-target="menu" class="mobile-menu-overlay hidden">
      <div class="flex items-center justify-between px-5 py-4 border-b border-white/10">
        <%= link_to root_path, class: "font-serif text-white font-bold text-xl tracking-tight",
            data: { action: "click->mobile-nav#close" } do %>
          The Magazine
        <% end %>
        <button class="flex items-center justify-center w-9 h-9 text-gray-400 hover:text-white transition-colors"
                data-action="click->mobile-nav#close"
                aria-label="Close menu">
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="6" x2="6"  y2="18"/>
            <line x1="6"  y1="6" x2="18" y2="18"/>
          </svg>
        </button>
      </div>
      <nav class="px-5 pt-4 pb-8 flex flex-col">
        <% mobile_link = "mobile-menu-link" %>
        <%= link_to "Home", root_path,
          class: "#{mobile_link} #{current_page?(root_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <% Category.joins(:articles).merge(Article.published).distinct.limit(5).each do |cat| %>
          <%= link_to cat.name, category_path(cat),
            class: "#{mobile_link} #{current_page?(category_path(cat)) ? 'text-white' : ''}",
            data: { action: "click->mobile-nav#close" } %>
        <% end %>
        <%= link_to "All Articles", articles_path,
          class: "#{mobile_link} #{current_page?(articles_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <%= link_to "Tags", tags_path,
          class: "#{mobile_link} #{current_page?(tags_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <%= link_to "About", about_path,
          class: "#{mobile_link} #{current_page?(about_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <%= link_to "Portfolio", portfolio_path,
          class: "#{mobile_link} #{current_page?(portfolio_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <%= link_to "Contact", contact_path,
          class: "#{mobile_link} #{current_page?(contact_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
        <%= link_to "Collections", collections_path,
          class: "#{mobile_link} #{current_page?(collections_path) ? 'text-white' : ''}",
          data: { action: "click->mobile-nav#close" } %>
      </nav>
    </div>


    <%# Spacer — takes the nav's height when nav becomes fixed, preventing content jump %>
    <div data-scroll-progress-target="spacer" style="height:0;" aria-hidden="true"></div>

    <%# Progress bar — fixed just below the nav when nav is fixed %>
    <div data-scroll-progress-target="bar"
         style="display:none;position:fixed;top:0;left:0;width:100%;height:3px;background:#ef4444;transform:scaleX(0);transform-origin:left;transition:transform 80ms linear;pointer-events:none;z-index:41;"
         aria-hidden="true"></div>

    <%# Main content %>
    <main class="max-w-7xl mx-auto px-4 sm:px-6 py-10">
      <%= yield %>
    </main>

    <%# ── Subscribe CTA ──────────────────────────────────────── %>
    <section id="subscribe-cta" class="bg-gray-900 mt-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 py-16 flex flex-col md:flex-row items-center justify-between gap-8">
        <div class="text-center md:text-left">
          <p class="text-xs font-bold tracking-[0.25em] uppercase text-red-400 mb-2">Newsletter</p>
          <h2 class="font-serif text-3xl sm:text-4xl font-bold text-white leading-tight mb-3">
            Stories delivered to<br class="hidden sm:block"> your inbox.
          </h2>
          <p class="text-gray-400 text-sm leading-relaxed max-w-md">
            Get the best articles, interviews, and ideas from The Magazine — curated weekly, no spam.
          </p>
        </div>
        <form class="flex flex-col sm:flex-row gap-3 w-full md:w-auto md:min-w-[380px]"
              onsubmit="return false;">
          <input type="email"
                 placeholder="Your email address"
                 class="flex-1 px-4 py-3 text-sm bg-white/10 border border-white/20 text-white placeholder-gray-400 focus:outline-none focus:border-red-400 transition-colors min-w-0">
          <button type="submit"
                  class="px-6 py-3 text-sm font-bold tracking-wide uppercase bg-red-500 text-white hover:bg-red-600 transition-colors shrink-0">
            Subscribe
          </button>
        </form>
      </div>
    </section>

    <%# Footer %>
    <footer class="bg-gray-900 text-gray-300 border-t border-white/10">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-10 border-b border-gray-700 pb-10">
          <div>
            <h2 class="font-serif text-2xl font-bold text-white mb-3">The Magazine</h2>
            <p class="text-sm leading-relaxed text-gray-400">
              Stories that matter, perspectives that inspire, ideas that endure.
            </p>
          </div>
          <div>
            <h4 class="text-xs font-semibold tracking-widest uppercase text-gray-500 mb-4">Categories</h4>
            <% footer_cats = Category.joins(:articles).merge(Article.published).distinct.to_a %>
            <ul class="space-y-2 text-sm">
              <% footer_cats.each_with_index do |cat, i| %>
                <li class="footer-cat-item"<%= i >= 10 ? ' style="display:none;"' : '' %>>
                  <%= link_to cat.name, category_path(cat), class: "hover:text-white transition-colors" %>
                </li>
              <% end %>
              <li><%= link_to "All Articles", articles_path, class: "hover:text-white transition-colors" %></li>
            </ul>
            <% if footer_cats.size > 10 %>
              <button id="footer-load-more"
                class="mt-3 text-xs text-gray-500 hover:text-white transition-colors underline underline-offset-2">
                Show more &darr;
              </button>
              <script>
                document.addEventListener('turbo:load', function() {
                  var btn = document.getElementById('footer-load-more');
                  if (!btn) return;
                  var items = document.querySelectorAll('.footer-cat-item');
                  var total = items.length;
                  var INITIAL = 10;
                  var expanded = false;
                  // Reset state on every load (handles Turbo cache restores)
                  for (var i = INITIAL; i < total; i++) { items[i].style.display = 'none'; }
                  btn.textContent = 'Show more \u2193';
                  btn.onclick = function() {
                    if (!expanded) {
                      for (var i = INITIAL; i < total; i++) { items[i].style.display = ''; }
                      btn.textContent = 'Show less \u2191';
                      expanded = true;
                    } else {
                      for (var i = INITIAL; i < total; i++) { items[i].style.display = 'none'; }
                      btn.textContent = 'Show more \u2193';
                      expanded = false;
                    }
                  };
                });
              </script>
            <% end %>
          </div>
          <div>
            <h4 class="text-xs font-semibold tracking-widest uppercase text-gray-500 mb-4">Tags</h4>
            <div class="flex flex-wrap gap-1.5">
              <% Tag.joins(:articles).merge(Article.published).distinct.limit(20).each do |tag| %>
                <%= link_to "##{tag.name}", tag_path(tag),
                      class: "text-xs text-gray-400 hover:text-white transition-colors" %>
              <% end %>
            </div>
          </div>
          <div>
            <h4 class="text-xs font-semibold tracking-widest uppercase text-gray-500 mb-4">Quick Links</h4>
            <ul class="space-y-2 text-sm">
              <li><%= link_to "About Us", about_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "Contact", contact_path, class: "hover:text-white transition-colors" %></li>
              <li><a href="#" class="hover:text-white transition-colors">Advertise</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Privacy Policy</a></li>
            </ul>
          </div>
        </div>
        <p class="text-xs text-gray-600 mt-6 text-center">
          &copy; <%= Date.today.year %> The Magazine. All rights reserved.
        </p>
      </div>
    </footer>

    <%# ── Back-to-top ──────────────────────────────────────── %>
    <button data-scroll-progress-target="btn"
            data-action="click->scroll-progress#scrollToTop"
            title="Back to top"
            style="display:none;"
            class="scroll-top-btn focus:outline-none">
      <svg width="44" height="44" viewBox="0 0 44 44" style="display:block;transform:rotate(-90deg);">
        <circle cx="22" cy="22" r="19" fill="#ffffff" stroke="#fecaca" stroke-width="2"/>
        <circle data-scroll-progress-target="arc"
                cx="22" cy="22" r="19"
                fill="none" stroke="#ef4444" stroke-width="3"
                stroke-linecap="round"
                stroke-dasharray="119.38"
                stroke-dashoffset="119.38"
                style="transition:stroke-dashoffset 80ms linear;"/>
      </svg>
      <span style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;">
        <span data-scroll-progress-target="pct"
              class="scroll-pct"
              style="font-size:10px;font-weight:700;line-height:1;color:#ef4444;font-family:inherit;letter-spacing:0;">0%</span>
        <span class="scroll-arr" style="display:none;align-items:center;justify-content:center;color:#ef4444;">
          <svg width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 15l7-7 7 7"/>
          </svg>
        </span>
      </span>
    </button>

    <%# ── Bookmark float button ───────────────────────────── %>
    <%= link_to bookmarks_path,
          data: { controller: "bookmark-float", scroll_progress_target: "bookmark" },
          class: "article-bookmark-float",
          aria: { label: "View bookmarks" } do %>
      <svg class="bookmark-icon-empty" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
      </svg>
      <svg class="bookmark-icon-filled" fill="currentColor" viewBox="0 0 24 24">
        <path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
      </svg>
    <% end %>

  </body>
</html>
