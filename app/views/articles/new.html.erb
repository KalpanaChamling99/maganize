<%# Page header %>
<div class="mb-8 pb-6 border-b border-gray-200 flex items-center justify-between">
  <div>
    <p class="text-xs font-semibold tracking-widest uppercase text-gray-400 mb-1">Articles</p>
    <h1 class="font-serif text-3xl font-bold">New Article</h1>
  </div>
  <%= link_to "← Back to articles", articles_path,
    class: "text-sm text-gray-400 hover:text-gray-900 transition-colors" %>
</div>

<div class="max-w-3xl">
  <%= render "form", article: @article %>
</div>
